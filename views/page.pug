extends layout

block content
  .container
    .section
      h6.section-header
        a(href="/page/" + page) Page #{page}

      each post in posts
        include includes/postSnippet

    include includes/pagination